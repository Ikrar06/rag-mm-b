# =============================================================================
# RAG Prototype — pinned requirements (May 2026)
# Python 3.12.x | CUDA 12.4.1 | Driver 595.x
#
# torch & torchvision di-install SEBELUM file ini via Dockerfile
# agar tidak tertimpa CPU wheel dari PyPI.
# GPU wheel: torch==2.5.1+cu124, torchvision==0.20.1+cu124
# =============================================================================

# -----------------------------------------------------------------------------
# Core Framework — LlamaIndex
# -----------------------------------------------------------------------------
llama-index-core==0.12.5
llama-index-vector-stores-qdrant==0.4.3
llama-index-embeddings-huggingface==0.4.2
llama-index-embeddings-text-embeddings-inference==0.3.1
llama-index-llms-ollama==0.4.3
llama-index-llms-openai-like==0.3.1
llama-index-postprocessor-flag-embedding-reranker==0.3.1
llama-index-readers-file==0.4.3

# -----------------------------------------------------------------------------
# Embedding & Reranker
# torch sudah di-install via Dockerfile (cu124 wheel), tidak perlu di sini.
# -----------------------------------------------------------------------------
sentence-transformers==3.3.1
FlagEmbedding==1.3.3

# transformers: pin ke 4.47.x — JANGAN naik ke 5.x.
# transformers 5.x butuh torch._dynamo (sonicmoe.py) yang crash di Python <3.11.0-final.
# 4.47.x sudah cukup untuk semua kebutuhan embedding + intent classifier.
transformers==4.47.1
tokenizers==0.21.0
huggingface-hub==0.27.1

# numpy 1.26.x — numpy 2.x masih ada breaking change dengan PaddlePaddle & beberapa lib OCR.
numpy==1.26.4

# -----------------------------------------------------------------------------
# Vector Database
# -----------------------------------------------------------------------------
qdrant-client==1.12.1

# -----------------------------------------------------------------------------
# Backend
# -----------------------------------------------------------------------------
fastapi==0.115.6
uvicorn[standard]==0.34.0
python-multipart==0.0.20

# -----------------------------------------------------------------------------
# LLM Client
# -----------------------------------------------------------------------------
openai==1.59.3

# -----------------------------------------------------------------------------
# OCR & PDF
# paddlepaddle-gpu di-install via Dockerfile (cu124 wheel).
# paddleocr: pin ke <3.1.0 — versi lebih baru kena bug PIR + oneDNN di beberapa setup.
# -----------------------------------------------------------------------------
paddleocr==2.9.1
pymupdf==1.24.14
pdf2image==1.17.0

# -----------------------------------------------------------------------------
# Document Parsing & Chunking
# -----------------------------------------------------------------------------
unstructured[pdf]==0.16.11
pdfplumber==0.11.4
markdownify==0.13.1
pillow==11.0.0

# -----------------------------------------------------------------------------
# Auth
# -----------------------------------------------------------------------------
python-jose[cryptography]==3.3.0
bcrypt==4.2.1
passlib[bcrypt]==1.7.4

# -----------------------------------------------------------------------------
# Rate limiting
# -----------------------------------------------------------------------------
slowapi==0.1.9

# -----------------------------------------------------------------------------
# Logging & Monitoring
# -----------------------------------------------------------------------------
structlog==24.4.0
prometheus-fastapi-instrumentator==7.0.0

# -----------------------------------------------------------------------------
# HTTP Client
# -----------------------------------------------------------------------------
httpx==0.28.1

# -----------------------------------------------------------------------------
# Database & Cache
# -----------------------------------------------------------------------------
sqlalchemy==2.0.36
psycopg2-binary==2.9.10
redis==5.2.1

# -----------------------------------------------------------------------------
# Utilities
# -----------------------------------------------------------------------------
python-dotenv==1.0.1
pydantic==2.10.4
pydantic-settings==2.7.0
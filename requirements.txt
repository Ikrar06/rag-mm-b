# =============================================================================
# RAG Prototype — pinned requirements (May 2026)
# Python 3.12.x | CUDA 12.6.3 | Driver 595.x
#
# torch & torchvision di-install SEBELUM file ini via Dockerfile:
#   torch==2.7.0+cu126, torchvision==0.22.0+cu126
# paddlepaddle-gpu di-install SEBELUM file ini via Dockerfile:
#   paddlepaddle-gpu==3.3.0 cu126
# =============================================================================

# -----------------------------------------------------------------------------
# Core Framework — LlamaIndex
# Versi aktual per Mei 2026 dari PyPI.
# llama-index-embeddings-huggingface 0.4.2 tidak pernah ada di PyPI
# (loncat dari 0.4.0 ke 0.5.0), pin ke 0.6.1 yang verified latest.
# -----------------------------------------------------------------------------
llama-index-core==0.14.13
llama-index-vector-stores-qdrant==0.10.1
llama-index-embeddings-huggingface==0.6.1
llama-index-embeddings-text-embeddings-inference==0.4.0
llama-index-llms-ollama==0.10.1
llama-index-llms-openai-like==0.3.1
llama-index-postprocessor-flag-embedding-reranker==0.5.0
llama-index-readers-file==0.6.0

# -----------------------------------------------------------------------------
# Embedding & Reranker
# torch sudah di-install via Dockerfile (cu126 wheel).
# -----------------------------------------------------------------------------
sentence-transformers==3.3.1
FlagEmbedding==1.4.0

# transformers: pin ke 4.47.x — JANGAN naik ke 5.x.
# transformers 5.x menarik torch._dynamo (sonicmoe.py) yang tidak stabil.
transformers==4.47.1
tokenizers==0.21.0
huggingface-hub==0.27.1

# numpy 1.26.x — numpy 2.x masih breaking change dengan PaddlePaddle & PaddleOCR.
numpy==1.26.4

# -----------------------------------------------------------------------------
# Vector Database
# -----------------------------------------------------------------------------
qdrant-client==1.12.1

# -----------------------------------------------------------------------------
# Backend
# -----------------------------------------------------------------------------
fastapi==0.115.6
uvicorn[standard]==0.34.0
python-multipart==0.0.20

# -----------------------------------------------------------------------------
# LLM Client
# -----------------------------------------------------------------------------
openai==1.59.3

# -----------------------------------------------------------------------------
# OCR & PDF
# paddlepaddle-gpu di-install via Dockerfile (cu126 wheel).
# paddleocr 2.10.0 support paddle 3.3.x.
# -----------------------------------------------------------------------------
paddleocr==3.0.3
pymupdf==1.24.14
pdf2image==1.17.0

# -----------------------------------------------------------------------------
# Document Parsing & Chunking
# -----------------------------------------------------------------------------
unstructured[pdf]==0.16.11
pdfplumber==0.11.4
markdownify==0.13.1
pillow==11.0.0

# -----------------------------------------------------------------------------
# Auth
# -----------------------------------------------------------------------------
python-jose[cryptography]==3.3.0
bcrypt==4.2.1
passlib[bcrypt]==1.7.4

# -----------------------------------------------------------------------------
# Rate limiting
# -----------------------------------------------------------------------------
slowapi==0.1.9

# -----------------------------------------------------------------------------
# Logging & Monitoring
# -----------------------------------------------------------------------------
structlog==24.4.0
prometheus-fastapi-instrumentator==7.0.0

# -----------------------------------------------------------------------------
# HTTP Client
# -----------------------------------------------------------------------------
httpx==0.28.1

# -----------------------------------------------------------------------------
# Database & Cache
# -----------------------------------------------------------------------------
sqlalchemy==2.0.36
psycopg2-binary==2.9.10
redis==5.2.1

# -----------------------------------------------------------------------------
# Utilities
# -----------------------------------------------------------------------------
python-dotenv==1.0.1
pydantic==2.10.4
pydantic-settings==2.7.0